"use strict"   // treat all JS code as newer version

// alert(3+3)  // we are using nodejs,not console browser so it will give no output

console.log(3
    +3);   // code readability should be high

    let name="hitesh"
    let age=18
    let isLoggedIn=false

    // number => 2^53
    // bigint
    // string =>""
    // boolean =>true/false
    // null=> standalone value   //let state=null(empty)
    // undefined=> letState;  // no value assign
    //symbol => used in react / used for uniqueness

    //object

console.log(typeof null)


